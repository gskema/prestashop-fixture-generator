# PrestaShop Fixture Generator

Generates a set of images that can be copy-pasted into PrestaShop directory (before installing)
to replace default images of dresses.

## Requirements

- **GraphicsMagick** or **ImageMagick**

## Usage

``` shell
npm install
gulp
```
